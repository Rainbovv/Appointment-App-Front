// admin module params

export enum adminContentTypes {
    PATIENT = "PATIENTS",
    PERSONAL = "PERSONAL"
}

export enum roleTypes {
    PATIENT = "PATIENT",
    ADMIN = "ADMIN",
    DOCTOR = "DOCTOR"
}